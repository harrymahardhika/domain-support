<?php

declare(strict_types=1);

return [
    'per_page' => 60,

    // Namespace prefixed to EnumTrait::translated() lookups, e.g. "app"
    // resolves __('app.value') instead of the bare __('value'). Null
    // preserves the bare-key lookup.
    'translation_namespace' => null,
];
